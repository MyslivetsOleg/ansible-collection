---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: yes
  become: yes

- name: restart cntlm
  ansible.builtin.service:
    name: "{{ cntlm_service_name }}"
    state: restarted
  become: yes
