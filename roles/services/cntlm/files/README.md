# files/

Положите сюда RPM-пакет cntlm перед запуском роли:

    cp /path/to/cntlm-*.rpm roles/cntlm/files/cntlm.rpm

Имя файла должно совпадать с переменной `cntlm_rpm_src` (по умолчанию: `files/cntlm.rpm`).
